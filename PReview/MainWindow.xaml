<Window x:Class="PReview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PReview"
        xmlns:vms="clr-namespace:PReview" 
        mc:Ignorable="d"
        Icon="{StaticResource GithubLogo}"
        d:DataContext="{d:DesignInstance Type=vms:GithubVM, IsDesignTimeCreatable=True}"
        Title="Review PRs plz"
        WindowStyle="None"
        ShowActivated="False"
        Width="0"
        Height="0"
        Background="Transparent"
        Visibility="Visible">
    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressValue="{Binding Total}" ProgressState="{Binding ProgressState}" Overlay="{Binding OverlayImage}" />
    </Window.TaskbarItemInfo>
    <Grid>

    </Grid>
</Window>
